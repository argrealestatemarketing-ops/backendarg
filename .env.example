PORT=39772
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_min_32_chars
JWT_EXPIRE=7d

# CORS Configuration
ALLOWED_ORIGINS=http://10.93.21.196:39772,http://10.0.2.2:39772

# Firebase Configuration
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket
FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id
FIREBASE_APP_ID=your_firebase_app_id

# Database Configuration (for future use)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=hr_attendance

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Session Configuration
SESSION_TIMEOUT=30m

# Rate Limiting (optional, for future implementation)
RATE_LIMIT_WINDOW_MS=15m
RATE_LIMIT_MAX_REQUESTS=100
DB_NAME=hr_attendance

# Fingerprint Access DB (READ ONLY)
# Path to a Microsoft Access file (.mdb or .accdb). Can be relative (from project root) or absolute.
# Example: ./fingerprint_db/zk_attendance.mdb
# Set this env var to enable fingerprint verification during login and test endpoint
FINGERPRINT_DB_PATH=./fingerprint_db/zk_attendance.mdb

# Employee Excel file (READ ONLY)
# Path to an Excel file (.xls, .xlsx). The first sheet will be used. First row is expected to contain headers.
# Example: ./data/employees.xls
# Set this env var to enable Excel-based user verification during login
EMPLOYEE_EXCEL_PATH=./data/employees.xls

